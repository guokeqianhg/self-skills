# PerfDog 知识库 + 双层 RAG 项目 · 面试问答话术

> 项目路径：`E:\perfdog_agent\perfdog_knowledge_datasets`
> 三层架构：上游知识库构建（perfdog_llm_wiki_starter）→ 下游双轨 RAG 检索（perfdog_dual_rag）→ 统一服务网关（perfdog_service_gateway）

---

## 〇、话术公式（所有回答遵循的结构）

1. **先定性难点**——一句话告诉面试官你抓住了本质："这里的工程难点在于 A 和 B"
2. **讲设计动机，带方案对比**——"我没有用 X（因为……），而是用 Y"（展示思考过备选方案及其风险）
3. **拆实现机制**——给出具体机制，关键术语加粗
4. **场景化推演**——用一个具体事件流把机制串成因果链，把 if-else 分支讲全
5. **一句话升华价值**——"这种设计，本质上是……/彻底解耦了……"

---

## 一、项目全貌速记（电梯陈述）

**一句话**：面向 PerfDog 性能诊断场景的领域知识库 + 双层 RAG 检索 + 服务化网关，把 300+ 篇非结构化专家资料加工成带证据链的结构化知识卡，再通过"本地规则检索 + Qdrant 混合向量检索"双轨后端为 AI 性能分析报告提供证据底座。

**架构图**：

```
专家知识库资料链接清单.md（种子）
  ↓
【上游】perfdog_llm_wiki_starter —— 知识库构建流水线
  01 清单解析入 MySQL sources 表 → webapp 人工编目
  → 02 Firecrawl 抓正文 / 02b B站字幕 → 03 正文校验
  → 04 LLM 抽知识卡 → 05 双层校验（规则+LLM语义） → 06 渲染 Wiki
  产出：cards_validated.jsonl（279 张卡）+ wiki/（281 篇）
  ↓
【下游】perfdog_dual_rag —— 检索与消费
  cards+wiki 对齐 → rag_docs.jsonl（语料层）→ Qdrant 索引（dense+sparse）
  → local / qdrant 双后端检索 → 回表补全 → agent_context / 报告
  ↓
【服务层】perfdog_service_gateway —— 统一暴露
  13 条 HTTP 路由 + 9 个 MCP 工具（stdio + 远程 HTTP）
  + MySQL 任务队列（租约/心跳/全局写锁）+ Docker compose
```

**核心功能清单**：
1. 知识来源管理（Markdown 清单 → MySQL 注册表 → webapp 人工编目）
2. 网页/视频内容抓取（Firecrawl、B 站字幕 ASR）
3. LLM 知识卡抽取（7 种卡片类型，JSON Schema + Pydantic 强约束，证据链 source_claims 带 ≤25 词原文摘录）
4. 双层校验（本地规则短路 + 批量 LLM 语义打标，内容哈希缓存）
5. 双轨 RAG 检索（BM25/规则 local 后端 + dense/sparse 混合 Qdrant 后端 + 二阶段精排）
6. 纯规则查询理解（结构化 EvidenceFrame）
7. 服务化（HTTP API + MCP，任务队列带租约/心跳/全局写锁）
8. 检索基准评估（hit@1 / hit@3 / MRR，对比 4 种后端）

**技术栈**：Python、Qdrant（本地文件模式）、fastembed（dense: `thenlper/gte-large` 1024 维 / sparse: `Qdrant/bm25`）、MySQL（注册表 + 任务队列）、Flask webapp、纯标准库 `ThreadingHTTPServer` 手写 MCP endpoint、Docker compose、Pydantic、LLM（抽卡 + 语义打标）。

---

## A. 项目概述类

### Q1：介绍一下这个项目，你负责什么？

面试官您好，这个项目本质上是要解决"**让 AI 做性能归因时，结论有据可依**"的问题。背景是我们在做 PerfDog 性能分析的 AI Agent，但直接让 LLM 自由发挥，它给出的归因结论经常似是而非、无法追溯出处。

我做的方案是把问题拆成三层：**上游**用一个"LLM 抽卡流水线"，把 300 多篇零散的官方文档、B 站视频字幕，加工成 279 张结构化的"知识卡"——每张卡描述一个现象模式、根因、验证方法或优化动作，并且**每一条论断都带原文证据摘录**，可以回溯到出处；**下游**是一个双轨 RAG 检索引擎，同一份知识卡建两套索引——一套是纯本地的 BM25 加业务规则打分，一套是 Qdrant 向量库里的稠密加稀疏双通道混合检索；**最外层**是一个统一服务网关，把全部能力同时封装成 HTTP API 和 MCP 工具，让 Agent 可以自主发现和调用。

我负责整体架构设计和核心链路实现。这个项目我最想强调的是：**它不是"切块扔向量库"的普通 RAG，而是先建了一层有质量阀门和证据链的知识资产，再谈检索**。

### Q2：为什么不直接切块进向量库，要做"知识卡"这一层？

面试官您好，这里的核心矛盾是：**原始文档是不可信、不可控、不可组合的，而性能归因对这三点要求都很高**。

我逐个说。第一是**可信性**：直接切块检索，返回的是一段段原文片段，AI 报告引用时你只能说"大概出自某篇文档"；而知识卡里每个论断都有 `evidence_excerpt` 字段——一段不超过 25 词的原文摘录，外加一个 `claim_type` 标注这句话是原文显式说的、我们转述的、还是模型推断的。**推断类论断会被单独标记、重点审核**，这是切块方案给不了的。

第二是**可控性**：原始文档质量参差，有的文章通篇是营销话术。抽卡之后我有机会加质量阀门——双层校验、P0/P1 优先级、人工审核状态，检索时低质量卡片可以加权或过滤。质量控制在数据进入索引**之前**完成，而不是寄希望于排序算法在检索时兜底。

第三是**可组合性**：一个性能问题的完整证据链是"现象→根因→验证→优化"四段。知识卡有类型字段——7 种枚举，检索时我按类型分组取 top_k，返回的就是一条结构化的证据链；而切块检索返回的是一堆同质文本块，LLM 拿到后还得自己拼凑逻辑。

代价是构建成本高了，但我认为**知识工程的成本花在上游，比花在下游每次查询时纠偏要便宜**。

---

## B. 检索核心类

### Q3："dual RAG" 的 dual 体现在哪？为什么这么设计？

面试官您好，dual 体现在四个层面，我由浅入深说。

第一层是**双检索后端**：`local` 后端是我自己手写的 BM25 加规则打分，零模型依赖；`qdrant` 后端走向量库。第二层是**双向量通道**：建库时每张卡同时编码成两种向量——1024 维的稠密向量负责语义，稀疏的 BM25 向量负责关键词。第三层是**双索引文本**：同一张卡我会生成两段文本，`dense_text` 是自然语言描述，喂给语义编码器；`sparse_text` 是把关键术语用竖线拼成的串，喂给 BM25——**同一份数据、两种视图**。第四层是**双阶段排序**：向量库粗排之后，默认再过一道本地规则精排。

为什么要这么设计？因为性能诊断的查询是**"双语混合"的**：用户会同时说"p99FrameTimeMs 超标"——这是必须字面精确命中的指标词，语义相近不算数——和"转场时有明显长尾卡顿"——这是自然语言，需要语义理解才能匹配到"低帧"或"Jank"这类术语。纯关键词检索处理不了后半句，纯向量检索会把前半句稀释掉，所以必须混合。

### Q4：混合检索的融合算法用的什么？原理是什么？

面试官您好，融合用的是 **RRF，Reciprocal Rank Fusion，倒数排名融合**。我先讲它解决什么问题，再讲原理。

问题是这样的：稠密检索返回的是余弦相似度，分值在 0 到 1 之间；稀疏 BM25 返回的分值可能是 3.2、7.8，量纲完全不同。**两个分数量纲不同，直接相加没有物理意义**，而归一化又会被极端值带偏。

RRF 的思路是**干脆不看分数，只看排名**。每张卡在某个通道里排第 r 名，就得 1/(k+r) 分，k 是个平滑常数，经典取值 60。举个例子：一张卡在稠密通道排第 1，得 1/61 分；在稀疏通道排第 5，得 1/65 分；两分相加就是它的融合分。排第 1 和第 2 的得分差距大，排第 50 和第 51 几乎没差别——这正好符合直觉，**头部名次值钱，长尾名次不值钱**。

它的好处是三无：**无需归一化、无需调权重、对任何异构通道都鲁棒**。融合之后还有二阶段精排：我把候选池捞出来，在池内重建 BM25，再叠加业务规则重新打分——比如查询里提到 Android 而卡片是 iOS 专属的要降权，P0 卡比 P1 卡加分，有人工审核背书的卡加分，带告警标记的卡减分。**向量负责"捞得全"，规则负责"排得准"**。

> 追问钩子：k 为什么取 60？——太小了头部权重过于集中，太大了趋于平均，60 是原论文在多个数据集上的经验值。

### Q5：查询理解怎么做的？为什么用纯规则不用 LLM？

面试官您好，这里的工程决策是：**检索热路径上不允许出现 LLM**。我先讲实现，再讲理由。

实现上，我写了一个纯规则的查询理解器，把用户的自由文本解析成一个结构化的 `EvidenceFrame`——证据帧，里面包含：指标家族（比如这句话涉及帧率族还是内存族指标）、正向反向标签（用户是在描述问题现象还是在描述优化效果）、场景（启动、转场、团战）、趋势、干预手段、假说域。关键设计是：**查询侧和文档侧共用同一个解析器**——建库时知识卡也过一遍同样的解析。这样检索打分就变成了两个结构化帧之间的对齐度计算，每个得分项都能打印出来，能单测。

为什么不用 LLM 做查询理解？三个理由。第一，**延迟和成本**：查询理解在每次检索的热路径上，LLM 调用是几百毫秒起步还有 token 费用，规则解析是微秒级。第二，**可解释性**：召回出 bad case 时，我需要能定位是"查询理解错了"还是"排序错了"，规则是白盒，LLM 是黑盒。第三，**领域词汇是有限的**：PerfDog 就 30 个标准指标，场景词表也就几十个，规则覆盖率足够，用 LLM 是杀鸡用牛刀。

这个决策背后是一个更大的原则：**热路径用确定性系统，离线路径用 LLM**。LLM 被我放在离线抽卡和校验环节，那里可以容忍延迟，也真正需要语义能力。

### Q6：有了 Qdrant 为什么还要保留 local 后端？

面试官您好，保留 local 后端有四个作用，我按重要性说。

第一是**基线对照**。这是我最看重的：向量检索到底比关键词检索强多少，不能靠信仰，要靠数据。我有一组基准评测集，同一批 case 分别跑 local、qdrant 原始召回、qdrant 加精排三种链路，对比 hit@1 和 MRR。如果向量库的提升撑不起它的运维成本，我就应该砍掉它——**先有基线，才谈得上"优化"两个字**。

第二是**回退保障**。Qdrant 是本地文件模式，索引文件损坏、或者 embedding 模型加载失败时，系统自动降级到 local，检索服务不中断。

第三是**调试工具**。local 的每个得分项——BM25 词项分、平台匹配分、优先级加权——都能逐项打印。线上出 bad case，我先跑 local 看规则判断是否合理，再跑 qdrant 看语义召回是否合理，定位问题快得多。

第四是**零依赖演示**。给别人演示或者跑 CI 时，不装 Qdrant 也能跑通核心逻辑。

---

## C. 数据工程类

### Q7：LLM 抽知识卡怎么保证输出质量？

面试官您好，这里的难点在于：**LLM 本质是生成模型，它会编造、会跑题、会格式漂移，而知识库要求零编造**。我设了四道防线，按流水线顺序说。

第一道是 **Schema 强约束**。我先用 Pydantic 定义知识卡的数据模型，导出 JSON Schema 写进提示词：卡片类型只允许 7 种枚举，指标字段只允许出现 30 个 PerfDog 标准指标——**词表封闭，模型不能自造词**。这一步把"生成问题"约束成了"填空问题"。

第二道是**证据链强制**。每个论断必须附两个字段：原文定位，和一段不超过 25 词的原文摘录，还要自我标注这个论断是原文显式写的、是转述、还是推断。**限制 25 词是为了防止模型整段抄原文凑数**；标注 claim_type 是为了让下游知道该用多大的信任度对待它。

第三道是**双层校验**。规则层先做确定性检查：字段完整性、指标是否在词表内、枚举值合法。语义层再让另一个 LLM 以"审核员"视角打标：这个论断的语气是事实还是推测、内容角色是否匹配卡片类型。**生成和审核用两个独立 prompt，避免既是运动员又是裁判员**。

第四道是**分级处置**。校验产出 P0/P1 优先级和 `auto_checked`/`needs_manual_review` 状态，低置信卡进人工审核队列，webapp 里可以人工改。**机器负责批量过滤，人只处理边界 case**。

### Q8：语义校验的性能怎么优化的？

面试官您好，这里的难点是：**校验要调 LLM，而 LLM 又慢又贵，几百张卡逐张串行调用是不可接受的**。最初版本一张卡约 57 秒，我做了三层优化降到 25 秒以内。

第一层是**本地规则短路**。我分析了一批校验结果，发现大量结论是确定性的——比如某字段为空、明显是工具引用——这些根本不需要语义理解。我把这些模式写成前置规则，**能本地判定的绝不出本地**。

第二层是**批量请求**。LLM 调用的开销大头在网络往返和模型启动，一次请求里塞 12 张卡让它批量打标，调用次数从 O(n) 降到 O(n/12)。这利用了"**LLM 的单次成本曲线是亚线性的**"——一次问 12 个问题远比问 12 次便宜。

第三层是**内容哈希缓存**。校验结果以卡片内容的哈希值为 key 做缓存，内容没变就直接复用，重跑流水线时只有真正改动的卡才会产生新的 LLM 费用。并且缓存带版本号，校验规则升级时 bump 版本，旧缓存自动失效，防止脏数据。

总结成一句话：**先把确定性判断从 LLM 卸载到规则，再把剩余的调用次数除以批量大小，最后用缓存把重复调用归零**。

### Q9：运行时注册表为什么从 jsonl/xlsx 迁到 MySQL？

面试官您好，这里的本质是：**当数据从"单人维护的配置文件"变成"多人协作的运行时状态"时，文件方案的三个硬伤就暴露了**。

第一是**并发写**。webapp 上线后多人同时编目来源，文件方案是"读全量、改内存、写全量"，两个人同时保存必有一个人的修改被覆盖，且毫无报错。MySQL 有事务和行级锁，这是它的本职工作。

第二是**更新粒度**。来源的状态字段有十几个——enabled、抓取状态、审核状态、优先级——文件方案改一个字段也要全量重写整个文件，MySQL 一条 UPDATE 精确到行。

第三是**生态复用**。服务化之后我需要一个后台任务队列来跑重建索引这类长任务。如果用文件方案就得额外引 Redis 或 RabbitMQ，而 MySQL 用"状态字段加租约时间戳"就能实现一个够用的轻量队列——**在已有组件上多借一个能力，比引入一个新组件的运维成本低得多**。

迁移时我做了两件兜底的事：旧文件保留为只读快照不入新流程，同时提供两条迁移路径——新旧 MySQL 直连同步，和从仓库快照冷启动恢复，保证任何接手的人都能把系统拉起来。

---

## D. 工程化/服务化类

### Q10：为什么同时提供 HTTP API 和 MCP？

面试官您好，这里的设计出发点是：**同一套能力，有两类完全不同的消费者，它们的交互模式不一样**。

HTTP API 服务的是**程序系统**——脚本、后端服务、自动化测试。它们的特点是调用方自己写死流程，要的是稳定的路由、明确的请求响应。

MCP 服务的是 **Agent**——比如 CodeBuddy、Claude Desktop 这类 AI 编程助手。MCP 的全称是 Model Context Protocol，它的关键能力是**工具自发现**：Agent 连上来先发一个 `tools/list`，我返回 9 个工具的名称、描述和参数 schema，Agent 的 LLM 读完描述后**自己决定**什么时候调哪个工具、传什么参数，然后发 `tools/call`。也就是说，编排逻辑不在我的代码里，而在 Agent 的模型里——**我只负责把能力描述清楚**。

实现上两者复用同一套 runtime，只是协议适配层不同。HTTP 版是单 endpoint 加 session header 的模式：先 initialize 握手换取 session id，后续请求带在 header 里，服务端按会话管理状态。另外我还提供 stdio 版——通过标准输入输出走 JSON-RPC，本地 IDE 配置一行命令就能挂上，完全不涉及网络。**程序接入用 HTTP，Agent 接入用 MCP，能力只有一份**。

### Q11：长任务怎么处理并发和故障？

面试官您好，这里的典型场景是"重建向量索引"——分钟级的长任务，而且执行期间检索流量还在进来。难点有三个：**并发互斥、故障恢复、切换无感知**，我分别处理。

并发互斥用**全局写锁**：重建任务开始前先抢锁，检索请求发现锁被持有就拒绝或等待，防止"读到写了一半的索引"。

故障恢复用 **MySQL 任务队列加租约机制**，原理我拆一下：任务入队时是 pending 状态；worker 领任务时不是直接删掉，而是把状态改成 running 并写入一个租约过期时间，比如 5 分钟后；worker 存活期间定期发心跳把租约往后顺延。**如果 worker 进程挂了，心跳停了，租约自然过期，其他 worker 看到一条"running 但租约已过期"的任务，就知道它是孤儿任务，可以重新领走**。这样不依赖任何外部组件就实现了至少一次执行语义。

切换无感知用**原子替换**：新索引先在临时目录完整构建，全部校验通过后，通过一个原子操作把正式目录切换过去——本地是目录替换，远程模式是切换 collection 的别名。**检索流量从头到尾读的都是某个完整版本的索引，不存在中间态**。

### Q12：Docker 部署形态怎么设计的？

面试官您好，部署上我设计了两种形态，对应"个人开发"和"团队共享"两个场景。

个人形态是一条 `docker compose up` 拉起五个服务：网关、webapp、MySQL、worker、Qdrant，新人第一天就能把全链路跑起来。

团队形态有个值得讲的坑：**怎么让多人共享同一份知识资产**。方案是共享 MySQL 存注册表、共享持久化卷存知识卡和索引产物。但这里有个 Docker 的经典陷阱——**volume 首次挂载时会把镜像里同路径的内容遮掉**，我镜像里明明打包了基线知识库，挂上卷就全看不见了。解法是做了一个 `asset_seed` 一次性初始化容器：首次启动时抢先把镜像内的产物解包灌入共享卷，再启动主服务。这个模式如果以后迁到 K8s，就等价翻译成 initContainer。

我也把已知限制明确写进了文档：网关目前没有鉴权、Qdrant 本地文件模式不支持多副本——**承认边界比假装没有边界更专业**。

---

## E. 评估类

### Q13：怎么验证检索效果？

面试官您好，我建立了三层验证体系，对应"**改之前知道基线、改之时发现回归、改之后量化收益**"。

第一层是**基准评测集**。我人工标注了一批用例：每条是一个模拟真实用户提问，加期望命中的卡片。评测指标用 hit@1、hit@3 和 MRR——MRR 是平均倒数排名，正确答案排第 1 得 1 分，排第 2 得 1/2 分，排第 3 得 1/3 分，**它奖励"把正确答案往前排"的系统，对头部敏感**。同一批用例跑四种后端配置对比，能量化每个优化的真实收益。

第二层是**冒烟回归**。每次改完代码，跑一个脚本对同一 case 分别过 local、qdrant 原始、qdrant 精排三条链路，校验 top1、top3 的一致性，改动引入回归能立刻发现。

第三层是**缓存版本化防自欺**。语义校验的结果有缓存，如果改了校验规则却复用旧缓存，评测结论就是脏的。所以缓存带版本号，规则变更就 bump 版本强制全量重算。

一句话总结：**检索系统最怕"感觉变好了"，我的原则是任何优化必须有评测集上的数字背书**。

---

## F. 权衡与挑战类

### Q14：项目中最重要的技术取舍是什么？

面试官您好，我挑三个说，每个都是"放弃了一个看起来更强的方案"。

第一个取舍是**热路径确定性、离线路径智能化**。查询理解和检索排序我坚持纯规则加传统 IR 算法，放弃了"全链路 LLM"的时髦方案。理由是热路径对延迟、成本、可解释性三者都敏感，而 LLM 在这三点上全是短板；反过来离线抽卡对延迟不敏感、对语义理解要求极高，那才是 LLM 的主场。**把每种技术放在它 economics 最好的位置**。

第二个取舍是**引入知识卡中间层**。这让构建成本翻了几倍，但换来证据可追溯和质量阀门。我坚信 RAG 系统的上限是数据质量决定的，不是排序算法决定的。

第三个取舍是**自研轻量组件而非引重框架**。任务队列用 MySQL 租约实现而不用 Celery 加 Redis，MCP server 用标准库手写而不用重框架。因为我们的部署环境是受限的内网，要求 docker 一键起，**每少一个依赖，交付和排障成本就低一分**。这不代表我排斥框架，而是匹配当前规模——如果任务量上来，我会平滑迁移到专业队列。

### Q15：遇到过什么印象深刻的难点？

面试官您好，我讲一个"**认识到组件边界并在架构上兜住它**"的例子。

Qdrant 我用的是本地文件持久化模式，它本质上是一个嵌入库，**不支持多进程并发写同一个索引文件**。这在团队共享部署时是个真实的雷：如果按常规思路把应用扩成多副本，两个副本同时写索引，文件就损坏了。

我的处理分三层：部署层面明确约束应用单副本，并在文档里写明原因；进程内用全局写锁把重建和检索的写路径串行化；索引构建走临时目录原子替换，即使构建中途进程被杀，旧索引也完好无损。

这个经历给我的启示是：**选型时不仅要评估组件的能力上限，更要评估它的失效模式，然后在架构上把失效路径一条条堵死**。

---

## G. 开放题

### Q16：如果继续做，你会怎么改进？

面试官您好，我按优先级说四个方向。

第一是**查询理解的混合升级**。现在规则解析失败或置信度低时就没有然后了，我计划对低置信查询走 LLM 兜底解析，规则依然是默认路径——**混合而不是替换，保住热路径的延迟底线**。

第二是**在线评估闭环**。目前评测靠离线标注集，下一步把 Agent 实际回答中引用卡片的采纳率回传，作为排序特征的反馈信号，让系统从真实使用中学习。

第三是**索引增量更新**。现在知识卡一变就全量重建索引，分钟级；按 card_id 做增量 upsert 可以做到秒级，配合 webhook 触发就能实现"资料新增即刻可检索"。

第四是**从证据底座到归因裁决**。目前系统明确定位"只给证据、不做裁决"，下游 LLM 自己推理。积累足够多案例后，可以在 agent_context 之上加一个轻量的归因推理层，把"现象→根因"的常见推理链固化下来。

---

## 附：使用建议

话术里刻意埋了"钩子"——Q4 的"k 取 60"、Q8 的"57 秒降到 25 秒"、Q15 的"索引文件损坏"，都是具体的、可追问的点。面试官追问时往下再讲一层（如 RRF 的 k 值为什么取 60），就显得是真做过而不是背的。
