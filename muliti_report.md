# 多报告 AI 性能分析报告：面试问答

## A. 项目整体与业务链路

### Q1：介绍一下这个多报告 AI 性能分析项目，你负责什么？

这个功能解决的是多份 PerfDog 性能报告放在一起分析的问题。常见场景是同一个游戏的新旧版本对比，看版本有没有性能回归；或者同一版本在高、中、低端机上的表现对比，找出最容易出问题的机型；也有同设备下不同游戏的横向对比。

以前测试同学拿到多份报告后，需要自己分别查看 FPS、Jank、内存、CPU、GPU、温度等指标，再判断哪些差异有业务意义。报告一多，容易出现两个问题：一是不同报告的测试条件不完全一致，人工很难判断能不能直接比较；二是即使看出某个版本帧率变差了，也不一定能继续定位到是 CPU、GPU、内存还是温度导致的。

我负责实现了多报告分析的核心链路。业务方通过 `POST /compareAnalyze` 传入 2 到 5 份报告的 `caseId`、对比类型和业务上下文，服务先快速返回“已受理”，后台再用 LangChain 创建 `CompareAgent`。Agent 通过 MCP 接入 PerfDog 的报告数据接口，模型会调用报告统计、时序数据、掉帧上下文、根因分析等工具，最后生成固定结构的 JSON 对比报告。

这份报告分成 9 个模块，包含总体结论、FPS、流畅度、内存、CPU/GPU、温度功耗、异常清单、根因分析和优化建议。我的重点工作不只是让模型“写出一份报告”，而是解决模型输出不稳定的问题：模型可以负责分析和归因，但数字、报告结构和回调结果不能完全依赖模型自己保证，后面专门加了数据校正、`Zod` 校验和重试机制。

### Q2：请求进来以后，整条链路是怎么跑的？

入口是 Fastify 的 `POST /compareAnalyze` 接口。接口层会先校验 `taskId`、`uid`、`compareType` 和报告列表。报告数量限制在 2 到 5 份，`caseId` 必须是正整数，`seq` 必须在 1 到 5 之间，并且要严格升序，避免前端表格列和后端报告顺序对不上。

校验通过后，接口不会等待模型分析完成，而是立即返回 `{ ret: 0, msg: "accepted" }`。因为一次多报告分析需要几分钟，如果 HTTP 请求一直挂着，网关超时、用户刷新页面或者调用方断开都会影响任务。后台通过 `runWithLogger` 异步启动 `CompareAgent.compare()`，同时把 `requestId`、`sessionId`、`uid` 放进日志上下文，后面查问题时可以把一次请求的完整链路串起来。

Agent 跑完后不会把流式内容逐段回调，而是由 `CompareCallbackWriter` 缓存最终结构化结果。正常结束时统一向业务方发一次 `status: "done"` 的 HTTP 回调；中途出错则发一次 `status: "failed"`。这个 writer 内部有 `settled` 标记，保证同一个任务不会既回调成功又回调失败。

这样接口层只负责受理和校验，Agent 负责分析，回调层负责交付结果，三部分职责比较清楚。

### Q3：为什么 `compareType` 要由业务方传，而不是让模型自己判断？

因为“能不能比较”和“应该按什么角度比较”本身就是业务信息，不能完全交给模型猜。

系统里支持 7 类对比：重复测试、跨版本、跨设备、跨平台、同设备跨游戏、混合对比和完全不相关的对比。比如两个报告的包名、机型和版本看起来很接近，模型可能会猜它们是同版本重复测试；但业务方实际上可能是在验证两个不同构建包之间的性能回归，这时应该按跨版本来分析。

所以接口层把数字类型映射成内部枚举，例如：

- `1` 映射为 `SAME_GAME_REPEAT_RUN`
- `2` 映射为 `SAME_GAME_CROSS_VERSION`
- `3` 映射为 `SAME_GAME_CROSS_DEVICE`
- `4` 映射为 `SAME_GAME_CROSS_PLATFORM`

这个枚举会直接写进分析 prompt。跨版本重点看指标是否回归，跨设备重点看性能瓶颈是否集中在低端机，跨平台则必须说明 Android 和 iOS 的指标口径差异。对于 `MIXED` 或 `UNRELATED`，报告会明确提示可比性有限，不能硬得出“哪个更好”的结论。

模型可以基于报告元信息补充判断，但不会覆盖业务方明确传入的对比类型。

### Q4：这个 Agent 是怎么拿到 PerfDog 数据的？为什么用 MCP？

这里通过 `@langchain/mcp-adapters` 里的 `MultiServerMCPClient` 接入 PerfDog MCP 服务。

MCP 可以理解为给大模型调用外部工具定义的一套统一协议。以前如果直接接 HTTP 接口，需要自己定义工具名称、参数结构、返回格式，再把这些内容拼进 prompt；用了 MCP 之后，PerfDog 服务会把工具及其参数 Schema 暴露出来，Agent 运行时通过 `mcp.getTools()` 获取工具列表，再交给 LangChain 的 `createAgent`。

模型在分析时可以自主决定调用哪些工具。例如它先调用 `perfdog_get_report_data` 获取报告的基础统计数据，再调用 `perfdog_get_drop_context` 看掉帧区间，或者调用 `perfdog_analyze_root_cause` 获取某段异常的关联指标和候选根因。

接入 MCP 时还做了一层 Schema 清洗。不同模型对工具参数 Schema 的支持不完全一致，尤其是 Claude 和部分 OpenAI 兼容网关，对 `$schema`、`format`、`anyOf` 这类字段比较敏感。代码里会通过 `sanitizeToolsSchema()` 清理不兼容字段，避免工具还没调用，模型请求就因为参数 Schema 不合法而报 400。

## B. 分析流程与知识库

### Q5：模型拿到多份报告后，是怎么进行分析的？

整个流程不是让模型直接看完数据就写结论，而是给它设计了一套分阶段的 SOP。

开始时是基础事实采集阶段。prompt 要求模型对每个 case 调用报告数据工具和标签统计工具，拿到平均 FPS、Low 1%、帧时间、Jank、内存、CPU、GPU、温度等基础指标。这里还会确定目标帧率，优先使用业务方传入的验收口径，没有时再根据报告信息推断。

拿到基础数据后，模型开始识别异常。例如平均帧率相对目标帧率明显偏低、BigJank 频率过高、内存斜率持续增长、GPU 峰值过高、温度达到风险阈值等。发现异常后不会马上写根因，而是进入下钻阶段，继续调用掉帧上下文、时序数据或者根因分析工具，确认异常发生在哪个时间段，同时看 CPU、GPU、内存和温度是否有联动。

等异常点都有证据后，模型才进入最终输出阶段，把前面已经确认的数据和归因整理成 9 个模块的 JSON。prompt 里明确要求最终输出阶段不要再反复调用工具，避免模型已经拿到足够数据后，还陷入“再确认一次”的循环。

这套流程的作用是让模型先采集事实，再做判断。否则模型很容易只看见某个平均指标，就直接给出“GPU 瓶颈”或者“内存泄漏”这种没有证据支撑的结论。

### Q6：为什么要在多报告分析里加入知识库检索？知识库会不会把模型带偏？

知识库不是用来替代本次报告数据的，而是用来给模型提供下钻方向。

比如模型发现某份报告存在“FPS 不达标，同时 GPU 利用率不高”的现象，这种情况可能是主线程阻塞、资源加载、渲染等待，也可能是其他问题。知识库里会有性能专家沉淀的现象卡，卡片里记录某类现象常见的根因、需要验证的指标和可选优化方式。模型可以参考这些卡片，决定下一步应该拉哪个时间段、重点看哪些指标。

不过知识库有一个风险：历史经验和本次数据不一定匹配。如果报告是 Android 问题，检索到 iOS 平台的卡片；或者卡片描述的是“GPU 满载导致掉帧”，实际报告是“GPU 低利用率但帧率低”，直接套用经验就会把模型带偏。

所以 prompt 里给知识库检索设置了比较严格的条件。所有报告整体达标，或者异常都只发生在启动前 3 秒时，会直接跳过知识库。跨版本、跨机型场景通常只查最严重报告一次；跨平台、跨游戏则按报告分别查。检索回来的卡片还要经过平台、指标锚点、观测模式等筛选。

知识库只负责提供候选方向，最终根因和优化建议必须以本次 MCP 工具返回的数据为依据。即使知识库调用失败，任务也会静默退回到普通的数据分析流程，不会因为知识库不可用导致整份报告失败。

## C. 数值准确性与平台口径

### Q7：模型会幻觉，怎么保证报告里几百个数字是对的？

这是多报告分析里最关键的一层。模型从工具里拿到数据后，需要把平均 FPS、P95 帧时间、Jank 频率、内存峰值、CPU/GPU 占用这些数字填进最终 JSON。模型本质上是生成文本的，它可能把 A 报告的数据写到 B 报告里，可能四舍五入不一致，也可能在没有拉到数据时补一个看起来合理的数。

单纯在 prompt 里写“禁止编造”不够，所以这里做了一个本地数值校正层。

模型输出报告后，程序会遍历整个会话中的 `ToolMessage`，把 `perfdog_get_report_data` 返回的原始数据重新解析出来。解析结果按 `caseId` 存到 `reportDataByCase` 里，里面有 `metadata`、`statistics`、`statistics_extra`、`trends` 等真实数据。然后程序用这些数据回填模块 2 到模块 6 的核心字段，包括：

- 平均 FPS、Low 1%、P95/P99 帧时间、帧时间方差
- 小卡顿、大卡顿、卡顿率、冻结次数
- 内存均值、峰值、增长速率
- CPU/GPU 平均值和峰值
- 温度、功耗等指标

模型生成的数字在这里相当于初稿，最终交付的数据以 MCP 工具结果为准。

如果某份报告没有拉到某个指标，程序会把对应字段置为 `null`，而不是保留模型输出的数字。前端可以显示“暂无数据”，但不能把没有依据的数据当作事实展示。

### Q8：派生指标和异常数据也是模型生成的吗？

不是。只要可以从原始数据明确计算出来，就尽量由程序本地计算。

例如 FPS 达标率是 `avgFps / targetFps * 100`，内存占用率是 `peakMB / totalDeviceMB * 100`。这些计算如果交给模型，不仅可能算错，还可能因为不同报告的目标帧率不同导致比较口径不一致，所以在 `normalizeCoreMetricsFromToolData()` 和 `normalizeMemoryOccupancy()` 里统一重算。

Low 0.1% 也不是直接相信模型输出。程序会从工具数据里找同源指标，并检查它是否满足 `0 < Low0.1% ≤ Low1% ≤ AvgFPS`。如果不满足，说明数据口径可能有问题，直接置空，不把异常值写进报告。

异常清单的处理也是同样思路。如果某份报告调用过 `perfdog_get_drop_context`，程序会读取其中的 `dropEvents`，按跌幅、持续时间和起始时间排序，重建异常项；如果没有调用这类工具，则保留模型基于已有数据写出的异常，避免因为“没有额外证据”就把模型已经识别到的异常全部清掉。

这里以前出现过一个问题：最早是无条件重建，但模型有时走的是 `perfdog_analyze_root_cause`，没有 `drop_context` 数据，结果重建后列表为空，把原异常全覆盖掉了。后面修复成“有证据才回填，没有证据就保留原结果”。

### Q9：Android、iOS、Windows 的指标口径不一样，怎么处理？

同一个指标在不同平台上并不一定是同一个东西。内存就是典型例子：Android 常用 PSS，iOS 通常使用 Xcode 口径，Windows 使用 Working Set。CPU 也一样，Android 上 `cpu_total` 和 `cpu_app` 是两个不同指标，不能因为名字接近就混着用。

所以代码里有一个平台路由逻辑 `resolvePlatformMetricSet()`。它先从报告元数据判断平台，再按平台给不同指标配置候选列表：

- Android 内存优先取 `memory_pss`，CPU 取 `cpu_total`，GPU 取 `gpu_usage`
- iOS 内存优先取 `memory_xcode`，GPU 取 `gpu_device`
- Windows 内存取 `memory_working_set`，GPU 取 `gpu0_sys_3D(0)`
- macOS 使用自己的 CPU、GPU 和温度指标集合

程序会按优先级从工具返回数据里寻找真实存在的指标，而不是让模型自行猜测。像降频、耗电速率这类字段，如果 MCP 工具没有统一、可靠的来源，代码会直接返回 `null`，不让模型根据温度或 CPU 峰值去推测一个布尔值。

这样做会让部分字段为空，但避免了跨平台比较时把不同口径的数字硬放在一起。

## D. JSON 输出与模型重试

### Q10：多报告报告有 9 个模块、很多字段，怎么保证 JSON 一定能被前端解析？

这里用 `Zod` 做运行时校验。`Zod` 可以理解为 TypeScript 里的数据模型校验库，作用类似 Python 里的 Pydantic。代码里定义了 `compareReportSchema`，把整份报告的字段结构、数组类型、可空字段、模块 8 的 `hypotheses`、模块 9 的 `items` 都约束下来。

模型输出后，不是直接回调，而是进入 `structuredOutputRetryMiddleware`。这个中间件只在模型最终收尾时工作，也就是当前 AIMessage 没有继续发起工具调用时。它会先从正文里提取 JSON；如果正文为空，再从 reasoning 内容里尝试提取。有些模型或网关会把最终答案放进 reasoning 字段，这一步可以避免因为正文为空直接丢结果。

拿到 JSON 后会先做一次轻量修复，再用 `compareReportSchema.safeParse()` 校验。轻量修复主要处理模型经常犯的结构错误，例如模块 8 本来应该是：

```json
{ "hypotheses": [] }
```

模型有时会直接返回数组：

```json
[]
```

这种可以由程序直接包装成正确结构，不需要浪费一次模型重试。

如果是字段缺失、类型不对、没有拉数据就直接写报告等问题，中间件会生成不同的纠错提示，要求模型重新输出。重试最多 2 次，避免模型无限循环。

### Q11：重试时怎么判断模型到底错在哪里？

结构化输出重试分了几种情况，不是统一提示“请重新输出 JSON”。

如果模型最后没有任何 PerfDog 工具调用记录，就说明它可能没有采集数据，直接编了一份报告。这种情况下会明确提示它回到数据采集阶段，先调用 MCP 工具。

如果 JSON 能解析出来，但 `Zod` 校验失败，中间件会把校验错误按字段路径整理出来。例如可能是 `module2_fps.perReport[1].avgFps` 类型不对，或者 `module8_rootCause` 少了 `hypotheses`。这些错误会拼进重试提示，让模型针对性修改。

如果正文和 reasoning 都提取不到 JSON，说明模型最后输出的是解释性文字、Markdown 或其他内容，就提示它基于已经拿到的工具数据直接组装 JSON，不要再输出分析过程。

这样做比简单重试更有效，因为模型知道自己是“没拿数据”“结构不对”还是“根本没有输出 JSON”。同时，重试只发生在最终输出阶段，不会重新拉取所有报告数据，避免一次格式错误就把整个分析过程从头跑一遍。

### Q12：不同模型对 JSON Schema、工具参数的支持不一样，怎么兼容？

CompareAgent 通过 `LLMFactory` 按 provider 创建模型。Claude 这类 Anthropic 模型支持原生 JSON Schema 输出，代码会把 `compareReportJsonSchemaForAnthropic` 传给模型，让模型在服务端约束下生成结构化 JSON。

其他走 OpenAI 兼容接口的模型，比如部分 Kimi、Qwen、Gemini 或内部网关模型，结构化输出能力不完全一致，所以主要依赖前面说的 `structuredOutputRetryMiddleware` 做应用层校验和重试。

工具参数也有兼容问题。MCP 工具的 Schema 可能带 `$schema`、`format`、`anyOf` 等字段，有些模型网关会因为这些字段直接拒绝请求。这里会在工具绑定前执行 `sanitizeToolsSchema()`，把不兼容的 Schema 写法转换成更通用的形式。

这部分看起来是模型接入细节，但实际上很重要。Agent 本身的逻辑没问题，如果工具 Schema 在模型层被拒绝，整个多报告分析根本无法开始。

## E. 结论一致性与性能控制

### Q13：数据校正后，根因和优化建议会不会还是基于模型原来的错误数据？

会，所以数值校正之后还有一层模块 8 和模块 9 的复审。

模块 8 是根因分析，模块 9 是优化建议。模型一开始写这两个模块时，看到的是它自己整理出来的数值；后面程序把 FPS、Jank、内存、CPU/GPU 等数字回填成真实工具数据后，可能出现结论和新数字不一致的问题。

这里会把校正前和校正后的高影响事实分别拍平成两个对象，再做 diff。比较的内容包括相关性等级、最优和最差报告、目标帧率、各模块的核心数值和评级、异常规则以及异常列表。

如果没有变化，就说明模型原本的数据和工具数据一致，直接保留结论。如果变化较少，会调用辅助模型做一次轻量判断，只问“这些变化是否影响模块 8 和模块 9 的结论方向”，返回 `KEEP` 或 `REVISE`。只有辅助模型认为结论需要修改，或者变化项很多时，才把校正后的事实交给主模型重写根因和优化建议。

这样不会因为一个小数点修正就整段重写，也不会让明显已经失效的优化建议继续留在最终报告里。

### Q14：如何防止模型反复调用工具，导致任务超时或成本过高？

多报告分析的时间预算比较紧。prompt 里规定目标 8 分钟完成，最长 15 分钟；数据采集、异常下钻和最终输出都有轮次预算。除了 prompt 约束，代码层也做了几层限制。

模型调用工具时会经过 `toolDedupeMiddleware`。这个中间件会把工具名和参数做稳定序列化，例如 `{ case_id: 1, metric: "fps" }` 和字段顺序不同但语义相同的参数，最终会被识别成同一次调用。如果模型对同一个工具传完全相同的参数，第二次不会真的请求 MCP，而是返回一条提示，告诉模型上文已经有结果，应该继续分析或换参数。

Agent 执行时还设置了 `recursionLimit: 30`。LangChain 的 Agent 每执行一次模型调用或工具调用都会推进图状态，这个上限相当于防止模型无限循环的硬限制。

另外对比 Agent 里没有使用 `todoListMiddleware`。这是实际测试后的取舍：部分长思维模型会把“更新待办事项”当成独立工作步骤，在每个分析阶段前多调用一次待办工具，分析流程的轮次和 reasoning token 会明显增加。多报告对比是一次性任务，SOP 已经写在系统 prompt 里，不需要额外的待办管理。

### Q15：上游模型或者 MCP 服务出现网络错误，怎么处理？

流式执行在 `streamOnce()` 里做了上游错误识别。不是所有错误都重试，只重试那些明显属于临时网络问题的情况，例如空响应、HTTP 5xx、`ECONNRESET`、`ETIMEDOUT`、连接被关闭等。

这里会沿着错误对象的 `cause` 链往下找，因为 LangChain、模型 SDK 和网关经常会把原始错误包装好几层。如果只是看最外层错误，通常只能看到“Agent 调用失败”，找不到真实原因。

识别为瞬态错误后，任务会做指数退避重试：第一次大约等 1 秒，第二次大约等 2 秒，再加一点随机抖动，避免多个任务同时失败后在同一时刻一起重试。最多重试 2 次。

重试时会重新创建 `AbortController`、writer proxy 和工具调用去重记录，避免上一轮失败流里的半截状态带到下一轮。Schema 校验失败不走这条网络重试路径，因为那是模型输出问题，应该走结构化输出重试，而不是原样再发一次请求。

## F. 设计取舍与后续优化

### Q16：这个功能为什么不支持断点续跑、对话记忆和人工审批？

因为多报告分析是一次性后台任务，和通用聊天 Agent 的形态不同。

通用聊天需要保存上下文、支持用户追问、可能需要人工确认操作，所以会挂 LangGraph 的 checkpointer、memory 工具和 HITL（Human In The Loop）中断恢复机制。多报告分析的输入是固定的：报告列表、对比类型和业务上下文；输出也是固定的：一份 JSON 对比报告。中间不需要用户交互，任务结束后也没有继续追问的状态。

所以 CompareAgent 不挂 checkpointer，不接 memory 和 skill 工具，只接 MCP 数据工具。这样可以减少存储开销，避免历史任务的记忆影响当前报告，也让模型可用工具更少、分析路径更稳定。

这不是说多报告不能做成对话式，而是当前业务先把“自动生成一份可信的对比报告”做成独立能力。后续如果产品需要“基于这份对比报告继续追问”，可以把最终 JSON 和工具调用历史交给聊天 Agent 继续使用，而不是把两种任务混在同一个 Agent 里。

### Q17：这个模块当前有哪些不足？如果继续做，你会优先优化什么？

现在的 SOP、轮次预算和部分跨字段规则，主要还是写在 prompt 里约束模型。例如哪些场景必须填写可比性说明、哪些报告不应该做跨报告结论，这些规则还没有全部做成本地强校验。后续可以在 `compareReportSchema` 外增加更强的业务校验，例如用 Zod 的 `superRefine` 做跨字段约束，模型即使没有遵守 prompt，也无法直接交付不符合业务规则的报告。

数据采集目前主要由 Agent 自主决定工具调用顺序。prompt 已经要求在基础采集阶段并行拉多份报告，但模型并不保证每次都能做到最优。后续可以把报告基础统计的采集前置成程序逻辑，先并发拉齐 2 到 5 份报告的数据，再把整理后的数据交给模型做判断。这样模型少走几轮工具调用，整体耗时会更稳定。

数值校正目前是白名单式的，代码里明确写了 FPS、内存、CPU、GPU、温度等字段如何从工具数据回填。以后 PerfDog 新增指标时，需要同步改校正代码。更好的方式是维护一份“报告字段到工具字段”的映射配置，让校正层按映射自动工作，新增指标时主要改配置。

还有一个需要注意的点是 `labelIds`。接口目前会接收并传入 prompt，但 Agent 还没有在本地对工具结果按 label 做二次过滤。如果业务方后续需要精确到标签级别的报告比较，这部分应该补到 MCP 查询参数和后处理逻辑中。
