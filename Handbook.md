# PerfDog Handbook 项目面试话术手册

> 使用方法：每段话术开头都有一句"难点在于……"的破题钩子，说完停顿——面试官点头就继续，他插嘴追问，答案本来就埋在下一段里。
> 话术结构统一为：**破题（难点是什么）→ 决策（我怎么做、为什么不那么做）→ 机制（完整因果链）→ 升华（一句话总结价值/边界）**。

---

## 〇、开场：项目总介

"面试官您好，这个项目要解决的是团队知识散落的问题——文档在 iWiki、问答在企微聊天记录、经验在同事脑子里，新人查不到，AI 问答也无据可依。

我的解法是把知识库收敛到一个 Git 仓库里，文档全部是带元信息的 Markdown，用 CI 做强制格式校验，让'写文档'和'写代码'走同一套评审、版本、审计流程。光有仓库只是存档，我在上面做了三层系统：一套自研的中英混合检索引擎，一个可以主动查阅资料的 Agent 问答，以及五条内容生产管线——包括从 iWiki 整树导入六十三篇官方文档，和把企微聊天记录自动蒸馏成文档。整个系统零云服务依赖，内网一台机器部署。"

### 追问：为什么不直接用现成的 Wiki（语雀/Confluence/iWiki）？

"三个原因。一是可追溯：Wiki 的修改历史和评审能力很弱，Git 天生带完整历史和合并请求评审。二是可被程序消费：Markdown 加元信息是纯文本结构化格式，搜索、AI、批量导入都能直接处理；Wiki 的内容锁在平台数据库里，取出来得走 API 甚至爬虫。三是不绑定平台：仓库随时能搬走，文档永远是自己的。本质上，Wiki 是'给人看的文档工具'，我要的是'人和 AI 都能直接消费的知识数据源'。"

### 追问：frontmatter 是什么？一门技术吗？

"不是技术也不是官方标准，是一个被广泛沿用的格式约定：文件最开头用两行 `---` 包起来的一段键值对，YAML 语法。Markdown 正文是给人看的，这段元信息是给程序看的——类型、标题、作者、时间。最早是静态网站生成器 Jekyll 的做法，后来 Hugo、MkDocs 都沿用了。在这个项目里它是人和程序之间的契约：写的人按约定填，程序按约定消费——分类、搜索加权、问答过滤、模板生成表单，一处元数据多处消费，谁违反约定 CI 就拦谁。"

---

## 一、检索模块（被问概率最高，挖得最深）

### 1.1 中文关键词检索怎么实现的

"这里的工程难点在于中文没有空格，程序天然不知道怎么切词，而现成方案在这个规模下都太重。

我没有引入分词器——jieba 这类又是新的依赖，而且分词结果不一定贴合业务词——而是用了整词保留加二元组切片的策略：一段中文整体作为一个查询词，同时切出所有两字片段作为兜底。打分上拉开档次：整词命中权重 2，二元组只有 0.5。这样既享受二元组的召回能力，又不会让'率排'这种垃圾片段干扰排序。

在此之上叠加 IDF 逆文档频率，公式是 log(1 + 文档总数/(1+命中文档数))。效果是'性能''优化'这种几十篇文档都有的词几乎不加分，'Jank'这种稀有词一命中就是高分。

然后是字段分级：标题乘 5、标签乘 4、摘要乘 3、正文乘 1——标题是作者对内容最强的承诺。

最后一个规则是连续短语奖励：查询词连续命中标题时按长度平方加分。这一条是修真实 badcase 加上的——有篇文档标题就叫'内存上限'，用户搜'内存上限'它却排不进前面，因为正文出现次数少、二元组对它不敏感。平方加分后，标题连续命中四个字直接锁定第一名。"

#### 追问：两字切片遇到三字词，不就切乱了吗？

"不会切没，因为不是'只切成两字'——整段原词先保留，两字片段是额外的兜底，权重只有整词的四分之一。真正会切出来的垃圾片段，比如'率排'，文档里几乎不会出现这两个字连着，不命中就是零分；而'排查'这种到处都有的片段，会被 IDF 自动降权。这套方案真正的短板是歧义——人名'周玲珑'切出'玲珑'会误伤'玲珑塔'——这是字面方案固有的天花板，靠打分压不掉，所以要向量语义检索做第二条路互补。"

### 1.2 为什么不自研到底，而是加了向量检索

"字面检索有个绕不过去的天花板：用户用语和文档用语不一致。用户搜'卡顿'，文档写的是'掉帧'，字面方案永远对不上。

所以我引入了第二条路：语义检索。模型选型有个硬约束——内网合规要求数据不能出去，所以我用 bge 中文模型的 WASM 版本跑在纯 Node 进程里，不需要 Python、不需要 GPU、不调外部 API。

实现上两个细节：一是向量生成时做了归一化，归一化之后余弦相似度就等于点积，查询比较只需一个 768 次乘加的循环；二是 bge 官方建议查询侧加一句指令前缀、文档侧不加，这是读官方文档才挖到的优化点。"

### 1.3 两路结果怎么融合（RRF + 重排）

"两路召回的分数量纲完全不同——关键词得分是几十几百，向量相似度是零到一——直接加权平均没法配权重。所以我用 RRF 倒数排名融合：每个候选的得分等于 1 除以（60 加它在榜单里的名次），两榜分数相加、按文档路径加正文去重。它只认名次不认分数，天然规避量纲问题，效果是双榜都靠前的候选必然最靠前。

融合后取前 18 名送 bge-reranker 精读重排。这里要说清 embedding 和 reranker 的本质区别：embedding 把查询和文档分别压缩成向量再比较，信息有损失；reranker 是把查询和文档拼在一起让模型整体判断，精度高一档但慢得多。所以它只能做终审不能全量用——18 这个数字就是精度和 CPU 耗时的平衡线。

最终留 8 个块按文档聚合，每篇最多取 3 块喂给大模型——给太多会撑爆模型上下文还稀释重点。

整条链路每一环都有降级：向量索引不存在退回纯关键词，reranker 挂了按融合序直接返回。原则是功能可以少，服务不能死。"

### 1.4 文档切块策略

"切块的难点在于：切太粗，一块里什么词都有，关键词信号被稀释；切太细，块脱离上下文就没意义。

我的做法是按 Markdown 一到三级标题的层级结构切，每个块携带完整标题路径，比如'安装及运行 大于 Windows 安装'——即使块正文里没出现父主题的关键词，检索和 AI 也知道它属于哪个主题，相当于子块继承了父块的语境。超过 700 字的块再按空行细分，700 这个数来自 reranker 的输入截断限制——长块后半截的相关句会被直接丢掉。文末的参考文献区整块排除，那是链接不是知识，进检索池只会添乱。"

### 1.5 向量索引怎么做增量更新

"embedding 是全系统最贵的操作，所以索引必须增量。我给每个块记录正文的 sha256 指纹，重建时和旧索引比对：指纹对得上直接复用旧向量，对不上才送模型重算，消失的指纹说明内容被删就丢弃。

关键设计是按内容而不是按位置判断变更——文章中间插一段，后面所有段落位置都变了但内容没变，按位置会全部重算，按内容就全部免算。唯一全量作废的点是换模型：不同模型的向量空间不兼容，所以元信息里锁死模型版本，版本一变全部重算。"

### 1.6 追问：为什么不用现成的搜索引擎（BM25/Elasticsearch）

"BM25 是搜索引擎的经典打分公式，我自研这套的核心思想——稀有词加权、按命中计分——就来自它，是简化定制版。不用 Elasticsearch 有三个原因：一是它是 Java 重型服务，为百万级文档设计，几百篇文档装它，运维成本远超收益；二是它假设输入已分好词，中文还得再接分词器；三是自研能加业务规则——字段权重、连续短语奖励、指定文档置顶，这些 BM25 公式里没有，在 ES 里要研究插件体系才能实现。我砍掉的是词频饱和曲线和长度归一化，这两样在小规模场景不划算。自研不是不懂现成的，是算过账的选择。"

---

## 二、Agent 问答模块

### 2.1 Agent 工具循环怎么实现的

"这里的工程难点在于：复杂问题第一眼检索的资料往往不够，而传统 RAG 搜一次就回答，不够就瞎猜。

我的做法是把单向的'检索加生成'改成循环：系统提示词里用自然语言约定一个暗号协议——AI 需要查资料时，输出一个 action 代码块，里面是要调用的工具和参数。程序每轮解析 AI 的输出：没有暗号，说明它认为信息够了，这段输出就是最终答案；有暗号，就执行对应工具——读文档、换词再搜、列目录——把结果以用户消息的角色追加到对话里，再调一次模型。最多三轮，用完强制总结。

这个设计里消息列表是单调增长的，AI 每轮都能看到自己之前想查什么、查到了什么，这是连续推理的基础。工具结果以用户角色塞入，因为模型 API 只有 system、user、assistant 三种角色，没有工具角色。

安全上有三处必须做：只允许读 md 文件；路径禁止两个点——否则 AI 输出上级目录就能读到密钥文件；内容截断六千字符防撑爆上下文。另外 temperature 压到 0.15，让它的格式输出守规矩——温度高了会把 JSON 写坏，而解析失败的兜底是把这段输出当普通回答返回，宁可少调一次工具，不让流程崩。"

#### 追问：为什么不用官方 function calling？

"两个原因。一是兼容性：我们的模型走公司内部网关，后端模型不固定，有的模型对 tools 字段支持不好甚至不支持，而输出特定格式文本所有模型都会。二是可调试性：提示词和输出全是纯文本，出了问题打印出来就能定位，不用解析结构化的 tool_calls 字段。"

#### 追问：和单轮 RAG 比的代价？

"慢和贵——每多一轮工具就多一次模型调用的延迟。缓解办法是前端实时推送每一步工具调用，用户能看到 AI 正在翻哪篇文档，等待感会低很多。换来的是多跳问题的回答质量，这是知识库问答的典型场景。"

### 2.2 多轮对话记忆怎么管的

"模型上下文有限，对话不能无限累积。我的方案是滑窗加滚动摘要：最近八轮十六条原文直传；溢出的部分调摘要接口压成两百字，提示词里强制保留三样——核心问题、结论、涉及过的文档路径，路径必须留，因为用户后面会问'刚才那篇文档里还说了什么'。摘要是滚动的：下次再溢出，旧摘要和新对话合并成一段新摘要。

这里修过一个真实 bug，我称之为缓冲盲区：旧实现为了防止边界对话被立刻压缩，加了一个缓冲带，结果第 17 到 20 条消息掉进缝里——不够新进不了原文区，又不够老没被送去压缩，AI 对这四条彻底失忆。这个 bug 是用户拿真实 case 逼问才发现的，教训是有状态的滑窗逻辑一定要画边界图验证，不能靠直觉。"

### 2.3 怎么控制幻觉

"坦白说幻觉没有银弹，我的思路是四层压制加后果兜底。

第一层，提示词硬约束：只依据给定资料回答，没有就明说'知识库暂无'。第二层也是最硬的一层，物理隔离：AI 生成的草稿默认是待确认状态，根本不进入检索池，人审核通过才进——防止它拿自己没审核过的产出回答别人，错误自我循环。第三层，强制出处：每个结论标注文档路径，前端渲染成可点弹窗，用户一点就能核对原文；必须标出处这个义务本身就压制瞎编，编不出处的内容它不敢写。第四层，低温：temperature 0.15，少自由发挥。

四层合起来的核心思想是：幻觉不能完全消灭，但可以做到可核对、不扩散。"

### 2.4 指代消解怎么做的

"用户追问'那相关的链接呢'，这句话本身没有任何可检索的词。我的做法是轻量扩展：问题少于 30 字时，自动拼接上一轮的用户问题再检索——'掉帧排查'加上'相关的链接呢'，检索立刻有了上下文。问题够长就不动，长问题自带上下文，拼了反而干扰。

更标准的方案是让模型先把追问改写成完整问题再检索，但要多一次调用、多几秒延迟。知识库问答的追问基本都是同主题的，拼接能拿到九成效果——这是零成本换效果的权衡。"

---

## 三、SSE 流式输出怎么实现的

"服务器和浏览器之间建立 SSE 通道，服务器主动推消息，前端边收边渲染。协议格式很朴素：每条消息就是 data 冒号加 JSON 加两个换行。

前端没用浏览器自带的 EventSource，因为它只支持 GET，而我的问答接口要 POST 带对话历史，还要自定义消息类型。所以用 fetch 拿到响应流的 reader，自己按行切分解析——代价是多写几十行解析代码，换来 POST 和自定义消息的自由。消息分三类：step 是 Agent 每一步工具调用，前端显示'正在翻阅某文档'的芯片；delta 是答案增量；done 是收尾加参考文档列表。

有一个要主动坦白的设计：答案是伪流式——Agent 循环全部跑完、拿到完整答案后，按 24 字 12 毫秒切片重放。这是有意取舍：只有拿到完整答案，才能做两件事——检测'知识库暂无'来自动登记知识缺口，以及统计实际引用了哪些文档做使用记账。真流式的函数我写了，这条链路没启用，保留了切换能力。"

---

## 四、工程架构

### 4.1 为什么零依赖：不用 Express/数据库/前端框架

"架构上最反直觉的决策是：不用 Express、不用数据库、前端不用框架。原因有三。

第一，供应链安全：npm 装一个框架连带进来上百个依赖包，任何一个被投毒——这类事件真实发生过，热门包作者账号被盗塞恶意代码——安装即中招。这个项目运行时依赖为零，clone 下来不用 npm install 直接跑。

第二，Git 把数据库的三样核心功能白送了：删除可恢复、合并请求就是审核流、提交历史就是审计日志。装数据库等于把 Git 干好的事重干一遍。

第三，部署运维：整个系统一个 Node 进程加一堆文件，备份就是 git push，迁移就是重新 clone。

代价是并发安全要自己管——这恰好是下一个可以展开讲的点。"

#### 追问：什么情况会回头引入 Express 和数据库？

"Express 的回头点：路由多到分发逻辑本身变复杂，或者需要中间件生态做鉴权限流。数据库的回头点：出现 Git 不擅长的需求——按任意字段组合查询、聚合统计、多人同时编辑同一篇文档。Git 擅长整个文件的版本管理，不擅长结构化查询，那时就该让数据库管数据、Git 只管文档正文。"

### 4.2 Git 写并发怎么解决的（重点难点题）

"这里的工程难点在于：前台发布和后台定时同步会并发改同一个 Git 工作区。

场景还原一下：用户点发布，commit 刚完成、push 还没发，每 60 秒的定时 git pull 恰好执行，把远端别人的提交拉下来，本地历史被快进——这时候 push 会被远端拒绝，因为两边历史已经分叉。后果是网页显示发布成功、远端其实没有，一个偶发的难复现的假成功。

我的解法是五行代码的 Promise 链队列：利用 Promise 的 then 必然等前一个完成才执行的特性，用一个变量永远指向最后一个任务，新任务接在尾巴后面，把并发请求转化为串行事件流。有一个细节很关键：链条上必须吞掉错误——某个任务失败不能断链，否则后面所有任务永远卡死。并且定时 pull 自己也进这个队列，这样分叉场景在物理上不可能发生。

这个方案的边界我很清楚：只在单进程内有效。将来多实例部署，就要换成文件锁或数据库锁——但在当前规模，这是满足需求的最小方案。"

### 4.3 前端为什么是单个 151KB 的 HTML 文件

"整个管理台六个功能页写在一个 151KB 的 HTML 文件里，九百行内联样式加一千九百行原生 JS。浏览器只认 HTML、CSS、原生 JS 三样东西，框架源码浏览器不认，中间必须插一道构建工序——几百个 npm 包、构建配置、编译等待。我跳过整条链：改完刷新即生效，新人改前端不用装任何环境。

我清楚它的保质期：没有组件化、没有类型检查，可维护性随规模下降，151KB 已接近极限，功能再翻倍就拆分引入轻量框架。这是一个明确知道终点在哪的取舍。"

### 4.4 部署架构？有没有用 Nginx

"没有。Node 进程直接对外提供服务，内网几十人一台机器，Node 自己的 http 模块就够了。Nginx 是将来对外开放时的标准前置——它做四件事：托管静态文件、反向代理、负载均衡、HTTPS 处理，出名的原因是事件驱动模型单进程扛几万连接。到时候统一域名入口、HTTPS、登录验证、限流都在它这层做，业务代码一行不改。我说'项目目前没有鉴权、改进方向是反代鉴权'，指的就是用 Nginx 这一层挡住未登录用户。"

---

## 五、企微沉淀模块

### 5.1 消息去重与进度管理（最能体现设计深度）

"这里的工程难点在于企微的消息通道是不可靠的：五秒不确认就重推，消息会迟到、会乱序。

三个对应设计。第一，按消息编号幂等去重，重推的直接吞掉。

第二，也是最容易被追问的——处理进度我按入库序号记，不按时间记。为什么：如果按时间记'处理到三点半'，一条三点二十五的迟到消息三点三十五才到，时间游标已经越过它，它就永远漏掉。按入库序号，每条消息入库时拿一个全局递增的编号，迟到消息拿的是更大的新编号，下一批必然被处理到。也不用文件行号——三十天清理会重写文件删掉旧行，行号全部错位。

第三，结转机制：一批消息读完时，最后一个话题可能还没闭合——有人提问还没人答——这几条不推进游标，下批重新参与，话题不被拦腰切断。

另外写入是先写临时文件再改名的原子操作，进程写一半被杀掉不会留下半个损坏文件。

本质上，这套设计承认上游不可靠，然后在存储层用幂等加顺序号把不可靠变成可靠——这是消息处理系统的通用思路。"

### 5.2 聊天记录怎么变成文档的

"四步：消息按会话分文件落盘；模型按话题分段，切成一段段完整的问答候选——这步的难点是话题边界，所以配合结转机制，没闭合的留给下批；模型把候选起草成文档草稿；然后必须人工审核——页面上能看原始聊天上下文、能编辑草稿、有认领软锁防止两个人同时审同一条——通过后走统一管线进正式库。

两个边界是刻意划的。一是验签解密在上游回调服务，本系统只收标准化明文——内网零依赖管理台不该持有企微密钥、不该暴露公网入口。二是原始聊天记录不进 Git 仓库，本机留三十天自动清理，进仓库的只有审核过的文档——聊天记录有隐私，文档才是知识。"

---

## 六、内容管线

### 6.1 五种入库方式如何收敛

"五个入口——AI 按描述生成、上传文件整理、模板填空、问答式录入、iWiki 导入——产出格式各异的草稿，但最终全部汇入同一条管线：元信息校验、git 提交、推送 main、失效文档缓存、增量重建向量索引。好处是治理不依赖人的自觉，人写的和 AI 生成的过同一道卡口，不存在 AI 通道走捷径。

另一个设计是'模板即配置'：前端新增文档的表单不是写死的，是读取模板文件的元信息字段和章节结构动态生成的。新增一种模板就是加一个模板文件，前端代码一行不用改。"

### 6.2 iWiki 整树导入怎么保证原子性

"难点是六十三篇文档的批量导入不能留半成品。

流程上：逐篇拉取，单篇失败不阻断——一篇网络超时不该毁掉整批；文件名带父页面前缀消歧，四个栏目都有'常见问题'，平铺后互不覆盖；一级页面额外生成索引文档；整批写盘后统一校验，任何一篇不过就把这批文件全部删除回滚，仓库恢复原样；全部通过才做一次提交、一次索引重建。已存在文件自动跳过，所以重跑是幂等的——网络中断后重跑同一命令就能续导。"

### 6.3 讲一个踩过的坑（YAML 注入）

"印象最深的一个坑：有的 iWiki 文档正文第一行是列表项，自动提取的摘要以短横线开头，写进文档头之后，YAML 解析器把它当列表语法，整个文件解析失败，一批全废。本质是摘要内容注入到了格式层。

修复双保险：提取摘要时剥掉行首列表符——治标；序列化时对特殊字符开头的值强制加引号——治本，以后再遇到任何特殊首字符都不会崩。

教训有两个：批量功能先小批量试跑；解析外部数据永远不信任输入格式。"

---

## 七、治理：知识库怎么不腐化

"知识库最大的风险不是建不起来，是三个月后烂掉。我做了四层机制。

第一，CI 左移：缺元信息、缺类型是错误，直接阻断合并；断链和超期未更新只是警告。分层的理由是全阻断会逼人绕流程，全提醒等于没提醒。而且断链在我们这不是错误，是'尚未编写的知识'——允许先放链接后补文档，把缺口显式留在库里。

第二，每周自动体检六项指标：规模、空壳文档、断链、保鲜、问答缺口、零查阅文档。超九十天的文档自动降级为'待验证'——状态一变它就退出了 AI 的资料池，体检摘要推企微机器人提醒团队。

第三，缺口闭环：AI 答不上来的问题自动登记，同一问题被问两次以上，管理台一键让 AI 起草补文档——用户的提问行为本身在驱动知识库补全。

第四，图片孤儿清理，两条铁律：只在保存、删除这类落盘动作时触发，编辑过程中绝不删——编辑可以反悔；必须全库零引用才删，扫描范围包括所有未发布的草稿——草稿里还引用着的图不能删。

四层合起来的核心思想：治理不靠人的自觉，靠流程里的自动卡口。"

---

## 八、收尾开放题

### 8.1 性能瓶颈在哪

"当前规模没有真实瓶颈，但三个已知点和预案我都清楚。一是全量扫文档目录：已有五秒缓存加写操作主动失效，再上量换流式读取或独立 worker。二是 reranker 是 CPU 大户，所以只让它精读 18 条候选，这是精度和耗时平衡后的数字。三是向量索引每次查询全量读文件：几千条几十毫秒无感，十万条变秒级——预案两步走，先给读索引加缓存，这是个已知待办；再上嵌入式向量库，不需要独立服务的那种；最后才考虑专业向量数据库。查询逻辑封装在单独文件里，替换时上层不用改。"

### 8.2 为什么不现在就上向量数据库

"向量数据库不是一个装进代码的库，是一个要单独安装、单独运行、单独照看的软件。引入它意味着多装一个服务、多管一个数据目录、多做一套备份、多处理一层启动依赖，有的还要连带装它的依赖组件——部署从三步变八步，运维成本是每天持续的。而我现在几千个块，暴力扫描几十毫秒。真到要换的那天也优先选嵌入式向量库——和 SQLite 一个道理，一个库文件加数据文件，不需要独立服务——重型方案是留给百万级数据量的。技术选型不是选最强的，是选当前规模下总成本最低的。"

### 8.3 权限和安全怎么做的

"当前是内网信任模型：服务没有登录鉴权，前提是部署在内网、只有团队成员能访问。但输入侧防护是有的：路径穿越拦截两个点，防读密钥文件；编辑接口有目录白名单，只允许改五个知识域；上传图片有类型白名单加 10MB 上限；Agent 读文档也有路径校验。Git 推送权限本身是第二道边界——能改仓库的人本来就有权限。改进方向是前面架 Nginx 做统一登录验证，业务代码不动。"

### 8.4 文档量到十万篇怎么办

"三层各自有升级路径。关键词搜索换 Meilisearch 这类轻量搜索引擎；向量搜索换嵌入式向量库或专业向量数据库，HNSW 索引让查询毫秒级、不随总量增长；服务形态上搜索服务与 Web 服务拆分，写操作的进程内队列换成跨进程的锁。这些都不是现在该做的——架构的价值不在于预判所有未来，在于每个替换点的边界都清晰，到时候改哪块都不牵连全身。"

---

## 九、快问快答（概念题速记）

| 问题 | 一句话回答 |
|---|---|
| frontmatter 是什么 | 文件开头 `---` 包住的 YAML 元信息，格式约定不是技术，Jekyll 发明的惯例 |
| BM25 是什么 | 搜索引擎经典打分公式：词频收益递减 + 稀有词加权 + 长文档惩罚；我的自研是它的简化定制版 |
| IDF 是什么 | 逆文档频率：一个词出现的文档越多权重越低，log(1 + N/(1+df)) |
| 向量/Embedding 是什么 | 把文字转成 768 个数字，意思相近的数字也相近；解决"卡顿"搜不到"掉帧" |
| RRF 是什么 | 倒数排名融合：得分 = 1/(60+名次)，两榜相加；只认名次不认分数，规避量纲 |
| Embedding 和 Reranker 的区别 | 前者分别压缩再比较（快、信息有损），后者拼在一起整体判断（准、慢）；所以一个做初筛一个做终审 |
| Agent 是什么 | 让 AI 在回答过程中可以主动调工具查资料，而不是只能答第一眼看到的内容 |
| 为什么不用 function calling | 内部网关后端模型不固定，文本协议所有模型都认识；且纯文本易调试 |
| SSE 是什么 | 服务器往浏览器单向推消息的协议；我用 fetch+reader 手动解析，因为 EventSource 只支持 GET |
| 为什么伪流式 | 要拿到完整答案才能做缺口检测和引用记账；真流式函数已写未启用 |
| Nginx 是什么 | Web 服务器软件：静态文件、反向代理、负载均衡、HTTPS；事件驱动单机扛几万连接 |
| 幂等是什么 | 同一操作执行多次和执行一次效果一样；企微重推的消息按编号去重就是幂等 |
| 为什么按入库序号不按时间记进度 | 重推会让消息迟到，时间游标会漏掉迟到消息；序号游标下迟到消息编号更大必然被处理 |
| 供应链风险是什么 | npm 依赖树上百个包，任一被投毒安装即中招；零依赖就没有攻击面 |
| Promise 队列原理 | then 必然等前一个完成才执行，变量指向最后一个任务，新任务接尾巴，天然串行 |
| status 门禁是什么 | AI 草稿默认"待确认"不进检索池，人审核才进；防止 AI 信自己的谣言 |
| 文档即代码是什么 | 用管代码的流程管文档：Git 版本、MR 评审、CI 校验、可回滚 |
| 唯一事实源是什么 | 仓库是什么样系统就呈现什么样，没有第二份数据，不存在两边不一致 |
| MCP 是什么 | Model Context Protocol，让 AI 应用以统一协议接外部工具/数据源的标准，走 JSON-RPC；我的 iWiki 导入器就是一个 MCP 客户端 |
| HNSW 是什么 | 向量数据库的主流索引算法，把向量组织成导航图，查询走捷径不用逐个比，百万级也是毫秒 |
| Prompt Injection 是什么 | 用户在输入里藏指令（如"忽略之前的规则"）劫持 AI；防不住就只能说"资料是数据不是指令" |
| ff-only 是什么 | git pull 的一种模式：只允许快进合并，历史分叉时直接报错而不是自动合并出冲突 |
| sha256 指纹 | 内容的哈希值，内容一样指纹必然一样；用它判断文档块变没变，与位置无关 |

---

## 十、效果与度量类（面试官爱问"怎么证明做得好"）

### 10.1 检索效果怎么验证的？有没有评测集？

"诚实讲：没有建正式的标注评测集，我的方式是 badcase 驱动开发。流程是：真实用户提问 → 发现结果不对 → 定位是拆词问题、权重问题还是切块问题 → 加规则 → 把这个 case 留下来当回归。几个关键规则都是这么来的：连续短语平方加分，来自'标题写着内存上限却排不到前面'的 case；块细分 700 字上限，来自长块稀释信号的 case。

上线前做过一轮整体验收：把 iWiki 官方文档六十三篇全量导入后，拿真实问题测，比如搜 Jank 相关问题，官方文档命中第一名。

评测集是已知不足，计划也明确：缺口登记里积累的真实提问就是现成的评测语料，攒到一定量后做成自动化回归集，每次改检索逻辑跑一遍防止劣化。"

### 10.2 上线效果怎么样？有人用吗？

"系统里有真实的使用统计在跑：每篇文档被 AI 引用过多少次、哪些问题没答上来。治理看板上有六个指标，其中'答出率'和'查阅热度'就是效果度量——答出率看问答成功率的变化趋势，热度看哪些文档真正被消费、哪些是零查阅的死文档。

推广策略上我有个观点：工具好用只是必要条件，关键是让贡献知识的成本足够低——五种录入方式、AI 起草人只要点确认、提问本身自动登记缺口，都是在降贡献成本。"

---

## 十一、提示词与模型类

### 11.1 提示词怎么设计的？怎么迭代的？

"系统提示词分四块结构：角色定义、工具协议说明、知识库自我认知、回答规则。

'知识库自我认知'这块值得单独讲：我把五个知识域各放什么、资料路径代表什么写进了提示词，因为发现 AI 被问'知识库里有什么''这些出自哪个文档'这类关于库本身的元问题时，会答得离谱——它分不清'知识库收录的文档'和'文档里引用的外部文章'。加了这个板块后元问题才答对。这就是迭代方式：不靠一次性写好，靠 badcase 驱动，每个修掉的 case 都在提示词里留下一条对应的规则。

另外还有几条具体规则有明确出处：'术语以术语表为准禁止自造词'来自团队规范；'资料里的图片链接必须原样保留'来自前端要渲染图片的需求；'资料不足就明说暂无'这条同时服务于防幻觉和缺口登记——答案里出现这句话会被程序检测到并自动记录。"

### 11.2 模型选型怎么考虑的？

"模型走公司内部网关，当前用 Kimi。选型三个考量：一是网关兼容性，文本协议不挑模型；二是指令遵循能力——Agent 的 action 暗号协议完全依赖模型遵守输出格式，这是硬指标，小模型容易把 JSON 写坏；三是中文能力，知识库全中文。

temperature 是分场景设的：问答和 Agent 循环 0.15，要稳；摘要压缩 0.1，要更忠实原文。没有一个参数是随便拍的。"

### 11.3 token 成本和延迟怎么控制？

"四面漏水的桶都要堵。输入侧：文档读取六千字符上限、喂给模型的摘录两千五、历史只带十六条、摘要两百字——每一处截断都是成本阀门。流程侧：Agent 硬上限三步，防止无限循环烧钱。调用侧：能不调就不调——指代消弁用拼接而不是改写，省一次调用；摘要是滚动合并而不是每次全量重压。延迟侧的体感优化是 SSE 把 Agent 每一步推到前端，用户看着 AI 翻资料，等待感就下来了。"

---

## 十二、安全类

### 12.1 提示词注入怎么防？（用户在问题里写"忽略之前的指令"）

"先承认现状：这是内网工具，用户是可信同事，目前没有做专门的注入防御。但已有三层间接缓解：一是资料池门禁，能进检索的内容都过了审核，攻击面只剩当前用户的输入；二是低温加出处强制，AI 被注入后乱说话会因为没有出处而露馅；三是回答规则里写死了'仅依据资料回答'。

如果要做正式防护，方向有两个：输入侧对用户问题做注入模式检测；结构侧把检索资料包在明确的分隔符里，并在提示词里声明'分隔符内是数据不是指令'——这是目前业界的主流缓解手段，但要说清楚，注入没有彻底的解法，只能提高成本。"

### 12.2 文档里的敏感信息（密码、密钥）被 AI 读出来怎么办？

"三道防线。第一道在入口：内容进库要走评审流程，敏感信息在 MR 评审时就该被拦下。第二道在读取：Agent 的读文件工具有硬限制——只允许 md 扩展名、禁止路径穿越，所以密钥文件、配置文件物理上读不到。第三道在边界：企微原始聊天记录不进 Git 仓库，只留本机三十天，从制度上减少敏感信息沉淀。"

---

## 十三、框架与产品对比类

### 13.1 为什么不用 LangChain / LlamaIndex / Dify 这些现成框架？

"三个原因。第一，依赖重量：LangChain 全家桶几百个包，和我零依赖的原则直接冲突，而我要的核心逻辑——拆词、打分、RRF、工具循环——总共几百行，自己写比学框架的抽象更快。第二，可控性：连续短语平方加分、指定文档置顶这种业务规则，在框架的抽象层里很难找到插进去的位置，自研就是改几行的事。第三，合规：Dify 这类平台要么 SaaS 要么自建整套服务，数据出内网是红线。

还有一个诚实的原因：自己写一遍 RRF、写一遍工具循环，比调框架的 API 学到的多得多。现在面试官你问哪一层我都能讲到代码，这就是证明。"

### 13.2 和飞书知识问答、腾讯 ima 这些产品比，差异在哪？

"两类差异。一是知识源：那些产品的知识源是它们自己的文档生态，而我要解决的问题恰恰是收编散落在 iWiki、企微、Git 的多源知识——这正是五条入库管线存在的原因。二是可控与合规：内部产品能把检索策略、审核门禁、数据边界都按团队规范定制，企微聊天记录这种敏感数据也不会出内网。本质区别：它们是通用产品，这是为这一个团队的知识流定制的系统。"

---

## 十四、数据一致性类

### 14.1 文档缓存、向量索引和磁盘文件，三者不一致怎么办？

"先讲三层各自的一致性策略。文档缓存：五秒 TTL 兜底，所有写操作和 git pull 后主动失效，所以最长五秒不一致。向量索引：按内容指纹重建，文档改了哪些块就只更新哪些块，文档删了它的块在下次重建时被清掉。查询侧还有一道保险：向量召回的结果要按路径回查文档，已经被删除或降级的文档在这一步被过滤——就算索引暂时滞后，脏数据也到不了 AI 嘴里。

原则是一句话：允许短暂不一致，但出口处必须过滤。"

### 14.2 每 60 秒自动 git pull，产生冲突怎么办？

"pull 用的是 ff-only 模式——只允许快进，历史一旦分叉就报错而不是自动合并。为什么不会分叉：所有本地写操作都走串行队列提交并立即推送，本地不会积压未推送的提交，所以 pull 永远是干净的快进。真出现 pull 失败——比如有人在别的机器直接改了仓库——就记录日志等下一轮，不会带冲突运行。这是用纪律换简单：让冲突在物理上不可能发生，而不是写复杂的冲突处理逻辑。"

### 14.3 两篇文档内容冲突，或者一篇过时了怎么办？

"分两层处理。回答层：检索会把多篇都给 AI，提示词要求每个结论标出处——如果两篇冲突，AI 如实呈现并各自标注，让人判断哪篇可信，系统不替人做仲裁。治理层：过时的文档靠保鲜机制收敛——超九十天没更新的自动降级为'待验证'，降级后就退出了检索池，等于先把可疑的旧知识隔离，等人复审。冲突的最终解法是让人发现冲突，而不是让系统假装没看见。"

---

## 十五、落地与反思类

### 15.1 怎么让团队愿意用、愿意写文档？

"知识库项目十做九死，死因都一样：录入成本太高，没人写。我的解法是三管齐下。一是把录入成本打下来：五种入口里四种有 AI 参与——描述生成、上传整理、问答填入、聊天蒸馏——人的角色从'写作者'降成'审核者'，点一下确认就完事。二是让消费反哺生产：提问没答案自动登记缺口，高频缺口一键起草，用户的提问行为本身就在驱动补全。三是流程上不增加任何新负担：写文档就是提 MR，评审就是同事平时用的代码评审，没有新系统要学。"

### 15.2 这个项目你最后悔/最想重做的设计是什么？

"两个。第一，检索应该先建评测集再调参。我是 badcase 驱动——出问题修问题——规则越加越多，虽然每条都有出处，但缺一个回归集保证新规则不破坏旧 case，这个债迟早要还。第二，多轮记忆的滑窗逻辑应该一开始就画状态图。缓冲盲区那个 bug——十七条到二十条消息凭空消失——纯粹是边界靠直觉没验证。教训沉淀成一句话：有状态的东西，先把状态和转移画出来再写代码。"

### 15.3 如果重做一遍，哪里会不一样？

"大方向不变——Git 即存储、零依赖、降级链这些决策至今看都是对的。会变的是顺序：先把评测集和缺口登记做了，再开始调检索；先画滑窗状态图，再写记忆压缩。也就是把'验证手段'前置到'功能开发'之前。第一遍是功能驱动，第二遍会是验证驱动。"

---

## 十六、MCP 与 iWiki 导入深挖

### 16.1 MCP 是什么？你们的导入怎么用的？

"MCP 是 Model Context Protocol，一个让 AI 应用以统一方式接入外部工具和数据源的开放协议，传输层是 JSON-RPC。iWiki 官方提供了 MCP 服务，我的导入器就是一个 MCP 客户端：HTTP 流式连接，Bearer 令牌鉴权，调用它暴露的工具——拉页面树、读文档、取附件下载地址。

接入时踩了三个坑值得讲。一是参数大小写和类型：拉页面树的参数必须是数字类型，文档 ID 是字符串，错了就静默失败。二是附件不能直接抓：图片 URL 直接请求拿到的是登录页 HTML，必须先调接口换一个五分钟的临时下载地址再下载。三是超时：大说明书默认三十秒超时不够，调到一百二十秒。这批导入最后六十三篇零失败，四百多张图全部落盘。"

### 16.2 导入的图片怎么处理的？

"图片统一下载到仓库的 assets 目录，文档里的引用改成本地相对路径，这样知识库自包含——不依赖 iWiki 的临时链接，过期也不怕。图片的生命周期也有人管：文档里删掉引用后，落盘动作会触发孤儿清理，全库含草稿零引用才物理删除。"

---

## 十七、行为与软技能类

### 17.1 这个项目遇到的最大阻力是什么？

"不是技术阻力，是习惯阻力：让大家把知识从 iWiki 和聊天记录里搬出来。我的应对不是发文要求，而是把迁移做成顺路的事——iWiki 官方文档我直接批量导完了，团队零成本获得初始内容；企微沉淀是自动起草人工确认，贡献者几乎不花额外时间。先让库里有东西、查得到东西，价值自己会说话。"

### 17.2 这个项目让你学到最多的是什么？

"三句话。第一，小规模系统的复杂度不在算法在取舍——几乎每个决策都是'当前规模下的最小够用方案加一条明确的升级路径'。第二，AI 系统的质量七分在工程三分在模型——门禁、降级、出处、审核，这些不性感的工程决定了系统可不可信。第三，真实用户的一个 badcase 胜过自己想象的一百个优化——我的每个重要规则都是被一个具体失败逼出来的。"
